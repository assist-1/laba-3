Здесь только файл test.cpp
Это связано с тем, что проект был написан на Visual studio
К файлу тестов нельзя сделать Makefile, точнее можно, но работать это не будет
Потому что все файлы с тестами, которые добавлялись при помощи #include "gtest/gtest.h",
имели относительные пути (по отношению к файлу test.cpp) 
То есть во все файлах прописанно: #include "gtest/<имя файла>", хотя данный файл лежит в одной папке с ней, чтобы скопилировать файл, надо чтобы все 
#include "gtest/<имя файла>" были заменены на #include "<имя файла>", но таких файлов очень много, без учета других папок, которые подключаеются аналогично 